/*var assert = require('assert'),
    app = require('./../app'),
    github = require('octonode'),
    client = github.client(),
    ghrepo = client.repo('joebadmo/indexer'),
    menuArray = [],
    currentConf = {
        "github": {
            "user": "joebadmo",
            "repo": "afdocs-test"
        }
    };

suite('buildMenu', function(){
    test('buildMenu should build menu from repo', function(done){
        app.buildMenu('/test/test-data', currentConf, ghrepo, menuArray, function (){
            assert.equal(menuArray.length, 3, 'menuArray should have 3 elements');
            done();
        });
    });
});*/
