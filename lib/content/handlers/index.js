module.exports = function( conf ) {
    var handleFile;

    // include all files in directory except 'index.js'

    handleFile = function( file ) {

    // pass to async.parallel with file contents

    };

    return handleFile;
};
