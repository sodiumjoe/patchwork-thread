/*
var should = require( 'should' )
  , handlers = require( '../lib/content/handlers' )
  ;

describe( 'content/handlers', function(){
    handlers( { path: 'something' }, function( err ) {
        console.log( 'done' );
    });
    it( 'should be tested' );
});
*/
